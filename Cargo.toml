[workspace]
members = [
    "rustmetaverse_types",
    "rustmetaverse_structured_data",
    "rustmetaverse_protocol",
    "rustmetaverse",
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.74"
license = "MIT OR Apache-2.0"
repository = "https://github.com/JesusOkay/rustmetaverse"
authors = ["rustmetaverse contributors"]
keywords = ["secondlife", "opensimulator", "virtual-world", "lludp", "protocol"]
categories = ["network-programming", "games"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
